<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wireless.memarize">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".pages.main.MainActivity" android:launchMode="singleTop"/>
        <activity android:name=".pages.register.RegisterActivity" />
        <activity android:name=".pages.landing.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.register.RegisterActivity2" />
        <activity android:name=".pages.login.LoginActivity" />
        <activity android:name=".pages.store.StoreActivity" />
        <activity android:name=".pages.play.ChapterActivity" />
        <activity android:name=".pages.play.QuestionActivity" />
        <activity android:name=".pages.play.SumScoreActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>